spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    initialization-mode: always
  jpa:
    show-sql: true
    open-in-view: true
    hibernate:
      ddl-auto: validate
  h2:
    console:
      enabled: true